apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: debox
  labels:
    app: backend
    team: debox
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      name: http
    - port: 9090
      targetPort: 9090
      name: metrics
  selector:
    app: backend
